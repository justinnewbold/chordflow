# Project Instructions

CRITICAL RULES - Apply to every project, every session:

1. After EVERY push to GitHub, ALWAYS provide the direct Pull Request link immediately (format: https://github.com/justinnewbold/[repo]/pull/[number])
2. Automatically merge every PR immediately after creating it — never wait for approval
3. Never skip the PR link — post it before moving to the next task
4. Cloud-first development only — never use local terminal commands
5. Always use React Native / Expo for mobile apps (cross-platform)
6. GitHub username: justinnewbold
